# 食迹项目 Cursor 开发规则

本规则用于约束 Cursor 在食迹（Shiji）项目中的行为，确保所有 AI 辅助操作符合项目开发规范。

---

## 一、通用规则

1. **所有输出文档必须使用中文**
   - 生成或修改 `README`、`docs/`、`openspec/`、接口文档等内容时，一律使用中文。
   - 如需使用英文术语（如 TDD、OpenSpec），应在中文上下文中给出简要解释。

2. **严格遵守 OpenSpec 规范**
   - 在提出实现方案、生成代码或调整接口前，必须优先阅读并参考：
     - `openspec/specs/` 下的规范文档
     - `openspec/changes/` 下的变更文档
   - 未在 Spec / Change 中定义或授权的功能，不得直接进入实现。

3. **遵循 TDD（测试驱动开发）**
   - 所有功能开发均应遵守顺序：**先写测试 → 再写实现 → 最后重构**。
   - 如用户未显式要求测试，也应主动给出对应测试建议或测试骨架。

4. **生成代码必须包含必要注释**
   - 对以下内容必须补充简洁清晰的中文注释：
     - AI 相关接口与调用流程
     - 复杂状态管理逻辑
     - 关键边界判断与异常处理
   - 注释应解释「为什么这样做」，而不是机械描述「代码在做什么」。

---

## 二、OpenSpec 使用规则

1. **Spec 的作用**
   - 描述系统长期稳定的行为与架构约束，是项目的「唯一事实来源」。
   - Cursor 在设计方案或评估实现时，必须优先对齐 Spec 描述的行为。

2. **Change 的作用**
   - 描述某次迭代中的具体变更（新功能、接口变更、行为调整等）。
   - Cursor 在辅助实现某个需求前，应先找到对应的 Change，并以此为边界。

3. **必须先写 Change 的场景**
   - 新增任何业务功能或接口。
   - 调整现有接口字段或行为语义。
   - 影响用户可见行为的逻辑变更。
   - 对 Spec 约定行为做例外处理或拓展。

> 若用户请求实现的功能尚未在 Spec / Change 中出现，Cursor 应提示先补充或更新 OpenSpec，避免直接跳过规范开发。

---

## 三、TDD 规则

1. **所有新增功能先写测试**
   - Cursor 生成实现前，应先生成或补充对应测试，并明确测试覆盖的业务场景。

2. **后端测试优先级**
   - 优先生成接口（Controller）测试与服务（Service）测试：
     - 验证请求/响应结构、状态码、主成功路径与关键异常路径。

3. **前端测试优先级**
   - 优先生成：
     - 页面状态测试（加载、成功、失败、空数据等）
     - 组件测试（重要组件的渲染与交互）
     - 关键业务流程测试（如：记录饮食 → 发起 AI 分析 → 查看结果）

4. **测试通过后再重构**
   - 实现代码应以通过测试为第一目标，再在测试保护下进行结构和命名的优化。

---

## 四、前端（Flutter）规则

1. **按业务模块划分**
   - 严格遵守 feature-first 目录结构：
     - 业务模块统一放在：`apps/mobile/lib/features/<feature>/`
     - 公共能力放在：`apps/mobile/lib/core/` 与 `apps/mobile/lib/app/`
   - Cursor 生成或修改前端代码时，必须放在对应业务模块目录中，禁止随意新增平铺目录。

2. **客户端不得直接调用第三方 AI 服务**
   - 不允许在 Flutter 客户端中直接调用 Gemini 或其他第三方 AI API。
   - 所有 AI 能力必须通过后端提供的 API 间接访问。

3. **安全与密钥管理**
   - 不在客户端代码中写入任何生产环境的 API Key 或敏感配置。

---

## 五、后端（Spring Boot）规则

1. **按业务模块划分，模块内再分层**
   - 按业务模块划分在 `com.shiji.api.modules.*` 下，例如：
     - `user`、`food_record`、`ai_analysis`、`health_report` 等。
   - 每个模块内部再次按分层结构组织：
     - `controller/`：接收请求、返回响应，不写复杂业务逻辑。
     - `service/`：承载业务编排与领域规则。
     - `repository/`：数据访问。
     - `model/entity|dto|vo/`：数据模型分层。

2. **后端统一负责 AI 编排**
   - 所有 AI 调用封装在后端基础设施与模块内：
     - AI 客户端与调用封装置于：`infrastructure/ai/` 或相关模块。
   - 不允许在 Controller 之外的任意随机位置直接调用第三方 AI 服务。

3. **新增接口时的要求**
   - 新增接口必须满足：
     - 已有对应 Spec / Change。
     - 已新增或更新测试（接口测试 / 服务测试）。
     - 已补充或更新接口文档说明（位置由项目约定，如 `docs/api/` 或 OpenSpec 中的接口描述）。

---

## 六、生成代码与文档时的行为约束

1. **生成代码**
   - 必须：
     - 遵守现有目录结构和模块边界。
     - 同时生成或更新对应测试文件。
     - 添加必要的中文注释（尤其是 AI 接口、状态管理和边界判断）。

2. **生成文档**
   - 文档内容必须使用中文，并与 OpenSpec 保持一致或引用 OpenSpec。
   - 当实现与文档不一致时，应建议优先更新规范（Spec / Change），再调整实现。

本规则文件应被视为 Cursor 在本仓库中的「行为约束清单」，在后续演进中，如有规则调整，应同步更新本文件与相关 OpenSpec 规范。
