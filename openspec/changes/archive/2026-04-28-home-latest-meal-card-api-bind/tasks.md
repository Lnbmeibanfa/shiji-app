## 1. 首页最近一餐数据接入

- [x] 1.1 在 `apps/mobile` 新增最近一餐响应模型（覆盖 `mealType`、`recordedAt`、`totalEstimatedCalories`、可空 `mood`）
- [x] 1.2 在客户端 repository 层新增最近一餐查询方法，调用 `GET /api/meal-records/latest`
- [x] 1.3 在首页状态管理中接入“进入即请求”流程，并维护 loading/data/error 三态

## 2. 最近记录区块渲染与交互

- [x] 2.1 最近一餐有数据时，卡片同时展示餐别与时间，并将热量按整数 `kcal` 渲染
- [x] 2.2 最近一餐 `data=null` 时，展示“去记录一餐”引导内容
- [x] 2.3 空态引导入口点击后导航至记录饮食页面（`RoutePaths.recordMeal`）
- [x] 2.4 请求失败时最近记录区块降级展示，且不影响首页其它区块可用

## 3. 测试与回归

- [x] 3.1 增加测试覆盖：进入首页触发最近一餐请求
- [x] 3.2 增加测试覆盖：有数据时展示餐别+时间+整数热量
- [x] 3.3 增加测试覆盖：`data=null` 时展示引导并支持跳转
- [x] 3.4 增加测试覆盖：接口失败时展示降级内容且页面不崩溃
