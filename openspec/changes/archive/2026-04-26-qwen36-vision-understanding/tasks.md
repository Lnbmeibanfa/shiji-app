## 1. 配置与基础设施

- [ ] 1.1 在 `services/api` 新增视觉模型配置项（API Key、base URL、model、timeout）并补充示例配置文件
- [ ] 1.2 实现 `qwen3.6-flash` 视觉调用客户端封装，包含请求构建、超时控制与响应解析入口
- [ ] 1.3 增加 prompt 模板与结构化输出约束，确保模型返回可解析 JSON

## 2. 业务接口实现

- [ ] 2.1 新增视觉识别 API（Controller + DTO），支持图片引用输入并返回结构化菜品/食材结果
- [ ] 2.2 实现 Service 层编排：调用模型客户端、解析结果、填充标准响应字段
- [ ] 2.3 将上游异常映射为统一 `AiErrorCode` 与标准错误响应

## 3. 流程对齐与回退语义

- [ ] 3.1 对齐 `record-meal-ai-flow` 的识别阶段语义：成功追加结果、失败回到 `idle` 并可重试
- [ ] 3.2 去除/隔离旧 stub 直出路径，保留受控兜底开关以支持紧急回退
- [ ] 3.3 增加可观测性日志与指标埋点（耗时、成功率、错误类型），并确保日志脱敏

## 4. 验证与发布准备

- [ ] 4.1 编写单元测试：模型响应解析、错误码映射、失败回退分支
- [ ] 4.2 编写集成测试：端到端验证视觉识别接口在成功/超时/上游错误场景下的行为
- [ ] 4.3 更新 `services/api/README.md` 的配置与调试说明，补充灰度启用与回滚步骤
