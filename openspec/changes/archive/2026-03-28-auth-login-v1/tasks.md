## 1. 认证模块基础搭建

- [x] 1.1 在后端新增 `modules/auth` 分层目录与基础类（controller/service/repository/model）
- [x] 1.2 新增登录相关实体与仓储映射（覆盖 `user_auth`、`user_session`、`sms_code_log`、`user_agreement_accept_log`）
- [x] 1.3 新增统一认证错误码与返回对象，补充参数校验 DTO

## 2. 测试先行（TDD）

- [x] 2.1 编写发送验证码测试：成功发送、1分钟频控拒绝、小时级次数上限拒绝
- [x] 2.2 编写验证码登录测试：成功、过期、错误次数超限、重复消费拦截
- [x] 2.3 编写协议前置测试：未同意禁止登录、同意后记录留痕
- [x] 2.4 编写会话测试：30天有效恢复成功、失效或过期恢复失败、单设备覆盖旧会话
- [x] 2.5 编写退出测试：全部退出后所有会话失效并记录 `logout_at`

## 3. 发送验证码流程实现

- [x] 3.1 实现发送验证码服务编排（手机号校验、频控检查、验证码生成）
- [x] 3.2 实现短信网关接口与本地 stub（先不接真实供应商）
- [x] 3.3 实现 `sms_code_log` 写入与状态初始化（有效期 1 分钟）
- [x] 3.4 提供发送验证码接口并对接统一异常处理

## 4. 验证码登录与用户建链实现

- [x] 4.1 实现验证码校验状态机（待验证、已验证、过期、失效）与错误次数上限 10 次
- [x] 4.2 实现协议同意前置校验与同意日志入库（支持仅手机号留痕）
- [x] 4.3 实现 `user_auth` 查询/创建逻辑（预留多认证类型扩展）
- [x] 4.4 实现登录成功后的用户链路：更新验证码状态、创建或关联用户、更新 `last_login_at`

## 5. 会话与鉴权实现

- [x] 5.1 实现 token 签发与哈希存储，创建 `user_session`（有效期 30 天）
- [x] 5.2 实现单设备策略：登录成功时失效同用户其他有效会话
- [x] 5.3 接入 Spring Security token 过滤链与鉴权上下文解析
- [x] 5.4 实现会话恢复接口（token 存在、状态有效、未过期才恢复）
- [x] 5.5 实现全部退出接口（失效全部会话并记录退出时间）

## 6. 联调与收尾

- [x] 6.1 增加登录接口文档与示例（请求/响应/错误码）
- [x] 6.2 完成本地联调：移动端发送验证码、登录、会话恢复、全部退出
- [x] 6.3 执行后端测试并修复问题，确保新增测试全部通过
